# bin-pwned
A fast CLI tool that scans your system binaries for known vulnerabilities (CVEs) using the OSV API. It detects vulnerable versions of executables in /usr/bin (or any directory you choose) and shows clear, color-coded results with safe/vulnerable status.
